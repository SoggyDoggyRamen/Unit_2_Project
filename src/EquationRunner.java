public class EquationRunner {
    public static void main(String[] args) {
            LinearEquation line = new LinearEquation(-1, 1, -2, -3);
            line.calculateAll();
            System.out.print(line);
    }
}
